#include "GUI/TextBox.h"

TextBox::TextBox(Vector2f relative_pos, int width, int height, Color background_color, Font font)
    : relative_pos(relative_pos), width(width), height(height), background_color(background_color), font(font)
{
    // Draw background

}
